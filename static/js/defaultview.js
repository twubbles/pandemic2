

if ($('#defaultview').length > 0) {
		require( ['datatables'], function( datatables ) {
			require( ['formfix'], function( formfix ) {
				if ($("input[name='Latitude']").length > 0) {
					require( ['geo'], function( geo ) {
				
					});
				};
			});
		});
	};
	
